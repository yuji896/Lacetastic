<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lacetastic">

        <!-- App starts at Setprofile -->
        <activity
            android:name=".Setprofile"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".RecentDesignsActivity" />
        <activity android:name=".Profilepage" />
        
        <!-- MAIN customization and 2D preview screens -->
        <activity android:name=".LanyardCustomizationActivity" />
        <activity android:name=".CustomizationPage" />
        <activity android:name=".PreviewActivity" />

        <!-- AI Generator -->
        <activity android:name=".LanyardImageGenerator" />

    </application>

</manifest>
